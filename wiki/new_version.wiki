#summary 正在开发中的emlog进度



==新增特性：==

 * wap功能 手机上的emlog 让你随身随地浏览博客书写心情（wap第一版暂不支持写日志，但可以写twitter）[完成]
 * tiwwter 功能 一句话博客 [完成]
 * 日志自动保存，你只管认真的写日志，其他的交给emlog吧。[完成]

==功能优化：==

 * 优化数据库备份功能，兼容性更好。使得备份体验和成功率更高 [完成]
 * 优化图片生成缩略图部分，提高兼容性可靠性 [完成]
 * 登录方式的改进，前台也可以进行简单的管理操作 [完成]
 * 验证码好看了点，字母数字混合 ：） [完成]
 * 附件上传，用户体验游所改善 [完成]

==BUG修复==


==数据库变更记录==

2.6.0 －－－>2.6.5

{{{
 CREATE TABLE em_twitter (
 id INT NOT NULL AUTO_INCREMENT ,
 content VARCHAR(255) NOT NULL ,
 date VARCHAR(10) NOT NULL ,
 PRIMARY KEY (id)
 );

ALTER TABLE `em_config` ADD `index_twnum` TINYINT( 3 ) UNSIGNED DEFAULT '0' NOT NULL AFTER `index_tagnum` ;

}}}
==初步定为5.1后发布2.6.5==